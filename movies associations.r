library(arules)
movies <- read.csv("C:/Users/Suresh/Downloads/my_movies.csv", header = T, colClasses = "factor")
rules <- apriori(movies)
summary(rules)
rules <- apriori(movies,parameter = list(minlen=2,maxlen=3,supp=.7))
inspect(rules)                 
library(arulesViz)
plot(rules)
plot(rules, method = "grouped")
plot(rules, method = "graph")

rules <- apriori(movies,parameter = list(minlen=3,maxlen=4,supp=.7))
inspect(rules)                 
library(arulesViz)
plot(rules)
plot(rules, method = "grouped")
plot(rules, method = "graph")
rules <- apriori(movies,parameter = list(minlen=3,maxlen=4,confidence = 0.05,supp=.7))
inspect(rules)                 
library(arulesViz)
plot(rules)
plot(rules, method = "grouped")
plot(rules, method = "graph")
